# Use this cell to try the contact-based manipulation framework
from contact_demo import (
    demonstrate_box_sliding,
    demonstrate_cylinder_rolling,
    demonstrate_box_pivoting,
    extract_dual_quaternions,
    control_robot_with_trajectory
)

# Uncomment one of these lines to run a demonstration
# box, box_ee_poses = demonstrate_box_sliding()
# cylinder, cylinder_ee_poses = demonstrate_cylinder_rolling()
# box, box_ee_poses = demonstrate_box_pivoting()

# Get dual quaternion representation
# box_dquats = extract_dual_quaternions(box_ee_poses)
# print("Example dual quaternion from box trajectory:", box_dquats[0])

# Control the robot using the trajectory
# Use this if you want to make the robot follow the trajectory
# control_robot_with_trajectory(box_ee_poses, init_env, secondary_object, duration=5.0)
